import { ArticleLayout } from '@/components/ArticleLayout'

export const meta = {
  author: 'Chris Breuer',
  date: 'Coming soon.',
  title: 'Meet tlsx',
  description: 'HTTPS for localhost.',
}

export default (props) => <ArticleLayout meta={meta} {...props} />

Certificates for your local machine. Zero-config & zero-setup.